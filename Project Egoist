
local plr = game.Players.LocalPlayer

local Version = "0.4"


print("Loading  Variables")
local char = plr.Character or plr.CharacterAdded:Wait()
local hum = char.Humanoid
local humr = char.HumanoidRootPart
local bp = plr.Backpack


local function updVariables()
	char = plr.Character or plr.CharacterAdded:Wait()
	if char then
		hum = char:FindFirstChild("Humanoid")
		if hum then
			humr = char.HumanoidRootPart
			bp = plr.Backpack
		end

	end

end

print("Loading Gui")


local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/SiriusSoftwareLtd/Rayfield/main/source.lua'))()

print('Loading Window')
local Window = Rayfield:CreateWindow({
	Name = "Qwenzy Hub",
	Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
	LoadingTitle = "Loading",
	LoadingSubtitle = "by qwenzy",
	Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

	DisableRayfieldPrompts = false,
	DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

	ConfigurationSaving = {
		Enabled = true,
		FolderName = "QwenzyHub", -- Create a custom folder for your hub/game
		FileName = "NewEgoSave"
	},

	Discord = {
		Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
		Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
		RememberJoins = true -- Set this to false to make them join the discord every time they load it up
	},

	KeySystem = false, -- Set this to true to use our key system
	KeySettings = {
		Title = "Untitled",
		Subtitle = "Key System",
		Note = "No method of obtaining the key is provided", -- Use this to tell the user how to get a key
		FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
		SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
		GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
		Key = {"Hello"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
	}
})

print("Loading Connections")

plr.CharacterAdded:Connect(function()
	updVariables()
end)
plr.Idled:Connect(function()
	updVariables()
	game:GetService("VirtualUser"):Button2Down(Vector2.new(), workspace.CurrentCamera.CFrame)
end)


print("Loading Functions")


local VirtualInputManager = game:GetService("VirtualInputManager")
local camera = workspace.CurrentCamera

-- Function that clicks once at given screen position (defaults to screen center)
local function ClickOnce(x, y)
	x = x or (camera.ViewportSize.X / 2)
	y = y or (camera.ViewportSize.Y / 2)

	VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
	task.wait(0.05)
	VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
end

-- Example usage: click the center of the screen





local Main = Window:CreateTab("Main", 4483362458) -- Title, Image

local _version = Main:CreateSection("Version: "..Version)

local LevelFarm = false
local SafeZone = false
local NoEffects = false

local speed = nil
local jump = nil

local function getPing()
	local stats = plr:FindFirstChild("NetworkClient"):FindFirstChild("Ping")
	if stats then
		return stats.Value / 1000 -- convert ms to seconds
	end

	-- fallback using PerformanceStats
	local pingStat = plr:FindFirstChild("PerformanceStats") and plr.PerformanceStats:FindFirstChild("Ping")
	if pingStat then
		local pingStr = pingStat.Text:match("%d+")
		return tonumber(pingStr or "100") / 1000
	end

	return 0.1 -- default to 100ms if not found
end


local function teleportToPredictedBall(ball)
	local ping = 1.5
	local velocity = ball.Velocity
	local predictedPosition = ball.Position + (velocity * ping)

	-- teleport
	humr.CFrame = CFrame.new(predictedPosition + Vector3.new(0, 3, 0)) -- add some Y so you don't fall through
end
local Divider1 = Main:CreateDivider()

local status = "Getting"

local walkSpeedBuff = 0
local jumpPowerBuff = 0


local _Speed = Main:CreateSlider({
	Name = "Speed Buff",
	Range = {0, 100},
	Increment = 1,
	Suffix = "Reccomend 10",
	CurrentValue = 0,
	Flag = "speed", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback = function(Value)
		walkSpeedBuff = Value		
	end,
})

local Jump = Main:CreateSlider({
	Name = "Jump Buff",
	Range = {0, 300},
	Increment = 2,
	Suffix = "Power",
	CurrentValue = 0,
	Flag = "jump", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback = function(Value)
		jumpPowerBuff = Value		
	end,
})

local specDivider1 = Main:CreateDivider()

local autostart = false


local _AutoStart = Main:CreateToggle({
	Name = "Auto Start",
	CurrentValue = false,
	Flag = "farm", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback = function(Value)
		updVariables()
		autostart = Value
		if Value then
			while wait() do
				updVariables()


				local isPlaying = function()
					if plr.TeamColor == BrickColor.new("White") then
						return false
					else
						return true
					end
				end

				local _Spawn = function(pos,team)

					print("getting teamplace")
					local teamplace = workspace.World.Map.Field.Locations:FindFirstChild("Team"..team.."Positions")
					print("teamplace jhere")
					if teamplace and humr  then
						print(pos)
						humr.CFrame = teamplace:FindFirstChild(pos).CFrame
						print("teleported")
					end

				end


				if autostart  then
					if  not isPlaying()  then

						local mainGui = plr.PlayerGui:FindFirstChild("MainGUI")

						if mainGui then
							local event =  mainGui["Handler (MainGUI)"]:FindFirstChild("RemoteEvent")
							local lobby = mainGui:FindFirstChild("Lobby")
							local positionSelect = mainGui:FindFirstChild("PositionSelect")

							local team1 = positionSelect:FindFirstChild("Team1")
							local team2 = positionSelect:FindFirstChild("Team2")
							local selectTeam = function(pos,team)

								local positionPlay = pos
								event:FireServer({
									Function = "SelectPosition";
									Team = team;
									PositionName = pos;
								})

								task.wait(.3)

								if isPlaying() then
									if lobby and positionSelect  then
										lobby.Visible = true
										mainGui.PositionSelect.Visible = false

									end

									print("Teleporting")
									--_Spawn(positionPlay,team)
								end


							end

							selectTeam("CF",1)

							task.wait(.1)
							if not isPlaying() then
								selectTeam("CF",2)

							end


							if positionSelect then
								if  team1 and team2 then
									for _,button in team1:GetChildren() do
										if not isPlaying() then
											local team = 1
											selectTeam(button.Name,team)

											print("Getting info")
											print("team: ".. team.. " ; name: "..button.Name)
											task.wait(.1)
										end
									end

									for _,button in team2:GetChildren() do
										if not isPlaying() then
											local team = 2
											selectTeam(button.Name,team)

											print("Getting info")
											print("team: ".. team.. " ; name: "..button.Name)
											task.wait(.1)
										end
									end
								end
							end


							task.wait(.01)
						end

					else
						print("Waiting")
						wait(.1)
					end
				else
					break
				end
			end
		end
	end,
})

local Farm = Main:CreateToggle({
	Name = "Auto Farm",
	CurrentValue = false,
	Flag = "farm", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback = function(Value)
		updVariables()
		LevelFarm = Value
		if Value then
			while wait() do
				updVariables()
				if LevelFarm  then
					if  plr.TeamColor ~= BrickColor.new("White") and char and hum and humr then

						print(status)
						print("Finding Ball")

						if status == "Getting" then

							print("Finding Ball")

							local ball =  workspace.World.Balls:FindFirstChild("Default")


							if ball then

								print("Catching ball")


								local BallPart = ball:FindFirstChild("BallPart")

								if BallPart then
									teleportToPredictedBall(ball.BallPart)
								else
									warn("Cant Catch")
								end
							else
								print("No ball found")
							end

							if char.Ball:FindFirstChild("Holding") then
								warn("Ball catched")
								status = "Holding"
							end

						elseif status == "Holding" then
							if char.Ball:FindFirstChild("Holding") then

								status = "Shooting"


								local cf 
								local shoot = false
								if plr.TeamColor == BrickColor.new("Persimmon") then
									print("U20")
									cf = CFrame.new(-343.781799, 92.3485107, -385.377777, -0.613885641, 5.36675877e-08, -0.789394975, 1.84116757e-08, 1, 5.36675877e-08, 0.789394975, 1.84116757e-08, -0.613885641)
									shoot = true

								elseif plr.TeamColor == BrickColor.new("Cyan") then
									print("Bluelock")
									cf = CFrame.new(-339.348999, 92.3471527, 436.601837, 0.619035125, 2.31662245e-08, -0.785363317, -3.75929545e-08, 1, -1.33867917e-10, 0.785363317, 2.9606996e-08, 0.619035125)
									shoot = true

								end

								if shoot then
									warn("Shooting")
									humr.CFrame = cf


									task.wait(.1)
									ClickOnce()
									task.wait(.3)


								end

								status = "Getting"
							else
								status = "Getting"
							end

						elseif status == "Shooting" then
							wait()

						end
					else
						print("Waiting To Start")
					end
				else
					break
				end
			end
		end
	end,
})

local specDivider2 = Main:CreateDivider()

--------------------------- SPEC-----------

local _RemoveBarriers = Main:CreateToggle({
	Name = "Remove Barriers",
	CurrentValue = false,
	Flag = "barriersToggle", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback = function(Value)
		updVariables()

		Value = not Value

		workspace.Barrier1.CanCollide = Value
		workspace.Barrier2.CanCollide = Value

		for i,v:Part in workspace.World.Map.Field.Barrier:GetDescendants() do
			if v:IsA("Part") or v:IsA("MeshPart") then
				v.CanCollide = Value
			end
		end

	end,
})

--------------------------------------------------------------------------

local Visuals = Window:CreateTab("Visuals", "rewind")


local Divider3 = Visuals:CreateDivider()


local hideblackscreen = false

local _HideBlackScreen = Visuals:CreateToggle({
	Name = "Hide Black Screen",
	CurrentValue = false,
	Flag = "blackscreentoggle", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
	Callback = function(Value)
		updVariables()
		hideblackscreen = Value

	end,
})


plr.PlayerGui.ChildAdded:Connect(function(child:ScreenGui)
	if hideblackscreen then
		if child.Name == "TransitionGUI" or child.Name == "BlackScreen" then
			child.Enabled = false
		end
	end

end)



local _deleteCollide = Main:CreateButton({
	Name = "Delete Collide Part",
	Callback = function()

		workspace.World.Map.Field.Locations.CollidePart:Remove()


	end,
})



--------------------------------------------------------------------------

local Other = Window:CreateTab("Other", "rewind")

local Divider2 = Other:CreateDivider()


local Hop = Other:CreateButton({
	Name = "Low Online Server Hop",
	Callback = function()
		local Http = game:GetService("HttpService")
		local TPS = game:GetService("TeleportService")
		local Api = "https://games.roblox.com/v1/games/"

		local _place = game.PlaceId
		local _servers = Api.._place.."/servers/Public?sortOrder=Asc&limit=100"
		function ListServers(cursor)
			local Raw = game:HttpGet(_servers .. ((cursor and "&cursor="..cursor) or ""))
			return Http:JSONDecode(Raw)
		end

		local Server, Next; 

		repeat
			local Servers = ListServers(Next)
			Server = Servers.data[1]
			Next = Servers.nextPageCursor
		until Server

		TPS:TeleportToPlaceInstance(_place,Server.id,game.Players.LocalPlayer)
	end,
})





while wait(.1) do
	updVariables()

	if char then
		if hum then
			local  ball = char.Info.SpeedChange:FindFirstChild("WithBallSpeed")
			local normal = char.Info.SpeedChange:FindFirstChild("Running")
			local stun = char.Info.SpeedChange:FindFirstChild("Stun")
			if normal then
				normal.Value = 40+walkSpeedBuff.." "..50+jumpPowerBuff
			end
			if ball then
				ball.Value = 35 +walkSpeedBuff.." "..55+jumpPowerBuff
			end
			if stun	 then
				stun.Value = 2.5 +walkSpeedBuff.." "..0+jumpPowerBuff
			end			



		end
	end
end
